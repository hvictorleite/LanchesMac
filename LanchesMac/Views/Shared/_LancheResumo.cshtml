@model Lanche

<div class="col-sm-6 col-md-4 col-lg-2">
    <p><img src="@(Model.ImagemUrl)" alt="@(Model.Nome)" class="img-fluid" /></p>
    <h3>@Model.Nome</h3>
    <h4>Preço: @Model.Preco.ToString("c")</h4>
    <p>@Model.DescricaoCurta</p>
</div>